# quick-bangs
The lightest DDG/Kagi bang implementation (plus custom bangs) that I could manage.


### Configuration
> All configuration occurs in index.html directly.

**Default Search Engine**: If no bangs are specified, this engine is used.
**Custom Bangs**: A series of key-value pairs (bang->search_engine) defining custom bangs.
